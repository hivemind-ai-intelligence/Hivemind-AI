{
  "functions": {
    "api/[...path].ts": {
      "runtime": "@vercel/node@3",
      "maxDuration": 30,
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "buildCommand": "pnpm --filter @workspace/hivemind run build",
  "outputDirectory": "artifacts/hivemind/dist"
}
