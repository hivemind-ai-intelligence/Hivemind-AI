{
  "buildCommand": "pnpm --filter @workspace/hivemind build && cp -r artifacts/hivemind/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
